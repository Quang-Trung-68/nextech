FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# Dùng `sh` vì bind mount ./frontend:/app có thể làm mất quyền +x trên host
ENTRYPOINT ["sh", "docker-entrypoint.sh"]
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
