# Copy thành .env ở root (cùng cấp docker-compose.yml) nếu muốn đổi user/mật khẩu DB.
# Docker Compose tự đọc file .env này để thay ${POSTGRES_*} trong docker-compose.yml.
POSTGRES_USER=nextech
POSTGRES_PASSWORD=nextech
POSTGRES_DB=nextech
